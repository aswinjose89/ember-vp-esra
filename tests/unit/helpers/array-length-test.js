import { arrayLength } from 'wb-ui-esra/helpers/array-length';
import { module, test } from 'qunit';

module('Unit | Helper | array length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayLength([42]);
  assert.ok(result);
});
