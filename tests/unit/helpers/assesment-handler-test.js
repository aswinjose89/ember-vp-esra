import { assesmentHandler } from 'wb-ui-esra/helpers/assesment-handler';
import { module, test } from 'qunit';

module('Unit | Helper | assesment handler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assesmentHandler([42]);
  assert.ok(result);
});
