import { picklistValue } from 'wb-ui-esra/helpers/picklist-value';
import { module, test } from 'qunit';

module('Unit | Helper | picklist value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = picklistValue([42]);
  assert.ok(result);
});
