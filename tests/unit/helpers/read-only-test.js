import { readonly } from 'wb-ui-esra/helpers/readonly';
import { module, test } from 'qunit';

module('Unit | Helper | readonly');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = readonly([42]);
  assert.ok(result);
});
