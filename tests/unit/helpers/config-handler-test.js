import { configHandler } from 'wb-ui-esra/helpers/config-handler';
import { module, test } from 'qunit';

module('Unit | Helper | config handler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = configHandler([42]);
  assert.ok(result);
});
