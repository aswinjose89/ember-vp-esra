import { uppercase } from 'wb-ui-esra/helpers/uppercase';
import { module, test } from 'qunit';

module('Unit | Helper | uppercase');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = uppercase([42]);
  assert.ok(result);
});
