import { listCreation } from 'wb-ui-esra/helpers/list-creation';
import { module, test } from 'qunit';

module('Unit | Helper | list creation');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listCreation([42]);
  assert.ok(result);
});
